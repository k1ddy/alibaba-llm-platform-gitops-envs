apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/name: ai-runtime
      app.kubernetes.io/part-of: alibaba-llm-platform

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
